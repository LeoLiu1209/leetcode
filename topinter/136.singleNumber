note : 
一個數xor 0還會是那個數字本身
一個數字xor自己，會等於0
例如: 0 ^ 3 ^ 4 ^ 3 = 4

class Solution {
    public int singleNumber(int[] nums) {
        int result = 0;
        for(int i : nums) {
            result  = result ^ i;
        }
        return result;
    }
}
